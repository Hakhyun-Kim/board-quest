# 판 위의 원정대 (Board Quest)

길을 골라 나아가고, 큰 판 위에서 **턴제로 겨루는 전술 원정** 게임. 모바일 우선 웹앱 (React + Vite + TypeScript + three.js/r3f).
자매작 **백층 던전**(https://github.com/Hakhyun-Kim/dungeon100)에서 파생 — 절차 생성·외부 에셋 금지·검증 가능성 원칙을 그대로 잇되, **실시간 액션 대신 턴제 전술**이 코어다.
설계·로드맵: `docs/DESIGN.md`.

## 문서 관리 원칙
- **작업을 마칠 때마다** 이 문서(구조·규칙 변경분)와 `docs/DESIGN.md`(체크리스트·시스템 설명)를 갱신할 것.
- 아래 "다음 후보" 섹션을 항상 최신으로 유지 — 끝낸 항목은 지우고, 새로 떠오른 아이디어를 우선순위와 함께 추가.

## 다음 후보 (우선순위)

> 확장 판단 기준 3가지(절차 생성 우선·재미·검증 가능)와 후보 평가는 `docs/DESIGN.md`의 **"확장 방향"** 참조.

1. **스킬 시스템** — 직업별 특기 1~2종(범위 공격·돌진·보호막·저격). MP 또는 쿨다운. 턴제의 핵심 재미이자 빌드 다양성의 출발점.
2. **전투 연출 보강** — 이동 경로 따라 미끄러지기, 피격 번쩍·데미지 숫자, 처치 연출, 카메라 살짝 흔들기. (지금은 위치 보간만 있음)
3. **밸런스 시뮬레이터** — 헤드리스로 전투 N판 자동 진행 → 승률·라운드 수·사망 분포 리포트. 전투 로직이 순수 함수라 바로 가능. (`src/dev/simBot.ts` 예정, 브라우저 콘솔 `__bqsim.start()`)
4. **적 AI 고도화** — 지형 활용(숲·언덕 선점), 집중 공격, 사제 우선 노리기, 후퇴 판단.
5. **지형 상호작용** — 높이차 사거리/피해 보정, 물가 밀어내기, 파괴 가능한 바위.
6. **원정 사건 노드** — 갈림길에 이벤트(용병 제안·함정·수수께끼) 추가. 백층 던전의 방 이벤트와 같은 계열.
7. **저장/이어하기** — 원정 중간 상태를 localStorage에 (`bq-run`).
8. **음악** — 절차 생성 BGM(지도/전투/승리). 백층 던전 `music.ts` 패턴 이식.
9. **바둑·오목식 미니게임** — 마을이나 특수 노드에서 즐기는 판 위 미니게임(원작 영감 회수).
10. **모바일 최적화** — 터치 타겟 크기, 세로 화면 카메라 맞춤, DPR 제한.

## 실행
- `npm install` — 최초 1회
- `npm run dev` — 개발 서버 (기본 포트 5176, `PORT` 환경변수로 변경 가능 / 백층 던전 5175와 동시 실행 가능)
- `npm run typecheck` — 타입 검사
- `npm run build` — 프로덕션 빌드

## 검증 (이 프로젝트의 기둥)
- **전투 로직은 순수 함수** (`src/lib/battle.ts`) — 렌더·React 없이 상태를 굴릴 수 있다. 밸런스는 추측이 아니라 **측정**으로 정한다.
- **DEV 훅 `window.__bq`** (프로덕션 제외): `phase()` / `state()`(전투 상태) / `journey()` / `party()` / `gold()` / `tile(x,y)`(3D 클릭과 동일 경로) / `travel(nodeId)`. 3D 레이캐스트를 흉내 내지 않고도 전 플로우를 자동 조작할 수 있다.
- **`?rafshim` 쿼리** — 숨김 탭(헤드리스 프리뷰)에서는 크롬이 rAF를 멈춰 3D가 안 그려진다. index.html의 개발용 심이 타이머 rAF + ResizeObserver 폴리필 + **`window.__pump(n)` 동기 프레임 구동** + **뷰포트 0×0이면 1280×800 가상 뷰포트 폴백**을 제공한다.
- 크롬 집중 스로틀링(오래 숨겨진 탭)에서는 `setTimeout`이 죽으므로, 자동 검증은 **MessageChannel 틱**(스로틀 면제) + `__pump`로 구동할 것. 단 **적 턴 자동 진행은 setTimeout 기반**이라 스로틀 상태에서는 느려진다 — 전투 자동 검증은 보이는 탭이 안정적.
- 렌더 확인이 필요하면 `canvas.getContext('webgl').readPixels`로 픽셀 샘플을 떠서 "실제로 뭔가 그려졌는지"를 검사할 수 있다 (스크린샷이 막힌 환경 대비).

## 구조

**기능별 1파일 원칙** — 화면·오버레이는 `src/ui/`에 기능 단위로 분리해 커밋도 기능 단위로 나눈다. 표시(JSX)는 컴포넌트가, 상태 전이·보상·효과음은 App 콜백이 담당한다.
**주의:** 자동화(검증 스크립트·향후 시뮬봇)가 DOM 클래스(`.choice-btn`/`.action-panel`/`.map-node`/`.big-btn`)를 셀렉터로 쓰므로 클래스명은 함부로 바꾸지 말 것.

- `src/App.tsx` — phase 상태 머신 + 배선: `title` → `journey`(길 선택) → 노드별 화면(`battle` / `town` / `treasure` / `camp`) → 다시 `journey` … → 보스 노드 승리 시 `ending`. 전투 중 적 차례는 useEffect가 550ms 간격으로 `foeAct`를 돌린다. 전투 종료 시 체력·레벨·경험치·소지품을 원정 상태로 반영.
- `src/lib/battle.ts` — **턴 엔진 (순수 함수, 이 게임의 심장)**. 라운드마다 속도 순으로 전원 1회 행동, 한 턴 = 이동(선택) + 행동 1회(공격/회복/아이템/대기). 공격 후 상대 사거리 안이면 **반격 60%**. 지형이 공격·방어를 보정. `createBattle` / `doMove`·`undoMove` / `doAttack` / `doHeal` / `doItem` / `doWait` / `foeAct`(적 AI) / `previewDamage`(UI 미리보기와 실제 판정이 같은 식).
- `src/lib/board.ts` — 전투 보드 절차 생성(덩어리 지형: 숲·언덕·물·바위) + **다익스트라 이동 범위**(지형 비용, 적은 통과 불가·아군은 통과 가능하나 정지 불가) + 경로 역추적.
- `src/lib/journey.ts` — 원정 지도 그래프 절차 생성 (단계별 1~3노드, 인접 단계만 연결, 들어오는 길 없는 노드 방지). 중반 **마을 보장**, 보스 직전 **정비 노드 보장**. 노드 종류: 전투/마을/보물/야영/보스.
- `src/lib/encounter.ts` — 단계(tier)에 맞춰 보드·적 편성·배치 생성. **난이도 곡선의 단일 출처** (적 수·레벨·종류 풀·보상 금화).
- `src/lib/units.ts` — 직업 9종(아군 4: 검사·궁수·사제·창병 / 적 5: 고블린·늑대·도적궁수·오크·오크대장) 스탯·성장·경험치.
- `src/lib/items.ts` — 회복약·폭탄(범위)·숫돌(공격 버프). 전투 중 '행동'으로 사용.
- `src/lib/rng.ts` — mulberry32 시드 난수 + `nextRand`(상태를 들고 다니는 순수 난수 — 전투 리플레이 가능).
- `src/lib/sound.ts` — Web Audio 합성 효과음(파일 0개). 음소거 `bq-muted`.
- `src/lib/store.ts` — useLocalStorage + `suspendPersistence`(시연·튜토리얼용 저장 격리 스위치). 키 접두사 `bq-`.
- `src/three/BattleScene.tsx` — 보드 렌더(지형 InstancedMesh·하이라이트 InstancedMesh·차례 링) + 말 배치·체력 바 + **클릭 판**(투명 평면 레이캐스트 → 타일 좌표). 화면 비율에 맞춰 보드 전체가 들어오게 카메라를 맞추는 `CameraRig` 포함.
- `src/three/Piece.tsx` — 말(유닛) — 직업별 실루엣을 절차 지오메트리로만 표현.
- `src/ui/Menu.tsx` — **표준 메뉴 선택 UI** (아래 규약 참조).
- `src/ui/BattleHud.tsx` — 차례 순서 바 · 유닛 카드 · **행동 메뉴** · 표적 미리보기(예상 피해·반격) · 전투 기록.
- `src/ui/JourneyScreen.tsx` — 원정 지도(SVG 길 + 노드 버튼 + 키보드 선택지).
- `src/ui/TownScreen.tsx` — 마을(회복·물자 구입·동료 영입).
- `src/ui/Screens.tsx` — 타이틀·전투 결과·보물·야영·엔딩.

## 표준 메뉴 선택 UI (`src/ui/Menu.tsx`)
백층 던전에서 검증된 규약을 그대로 이식했다. 모든 선택 화면에서:
- **항상 한 항목이 하이라이트**(`.focused` 금색 테두리, 기본 = 첫 활성 항목)
- **방향키로 이동**(순환·비활성 스킵), **Enter/Space로 확정**, 숫자 1~9는 바로 선택
- 마우스 호버는 하이라이트와 동기화, 클릭은 즉시 확정
- 메뉴가 겹치면 **나중에 열린 메뉴만 키를 받음**(모듈 레벨 메뉴 스택)
- 자동 반복(`e.repeat`) 무시

## 원칙
- UI 문구는 **한국어**. 백엔드 없음 — localStorage만.
- **외부 에셋 금지** — 절차 생성 지오메트리 + 이모지/캔버스 + Web Audio 합성으로 해결. 부득이 추가 시 출처·라이선스를 문서에 기록. (폰트 Jua만 예외 — SIL OFL)
- **게임 로직은 렌더와 분리된 순수 함수 지향**. 전투·지도·편성은 React 없이 돌 수 있어야 한다 (시뮬레이터 전제).
- **시드 고정** — 같은 시드는 같은 지도·같은 보드. 재현 가능해야 디버깅도 밸런싱도 된다.
- 커밋 기록이 곧 작업 증빙 — 작업 단위마다 의미 있는 **한국어 커밋 메시지**로 커밋하고, 작업이 끝나면 바로 push.

## 관련
- 자매작(원작): **백층 던전** — https://github.com/Hakhyun-Kim/dungeon100 (실시간 액션 로그라이크). 절차 생성·검증 패턴·표준 메뉴 UI·rafshim 검증 심을 여기로 이식했다.
