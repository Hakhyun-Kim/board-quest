# 앞으로 할 만한 것 (Roadmap)

우선순위대로. 각 항목은 `docs/DESIGN.md`의 **확장 판단 기준 3가지**(절차 생성 우선 · 재미 · 검증 가능)를 통과한 것들이다.
짧게 끝나는 것과 오래 걸리는 것을 섞어 두었으니, 한 번에 하나씩 골라 커밋 단위로 끝내면 된다.

## 완료 (최근 → 과거)
- **v0.6 적 특기** — 도적궁수 저격 · 오크 강타 · 오크대장 포효. `foeAct`가 기본 공격과 통합 평가. 넣고 완주율 재조정(35→16→40%).
- **v0.5 밸런스 조정** — 완주율 0% → 약 35%. 경험치 전원 분배 · 레벨업 비용 인하 · 적 레벨/수·보스 완화.
- **v0.4 시뮬레이터** — `src/dev/simBot.ts`. `__bqsim.start/sweep/campaign`. node로도 실행. 200판 ≈ 50ms.
- **v0.3 특기 시스템** — 직업당 1종·쿨다운제. `src/lib/skills.ts` + `doSkill`.
- **v0.2 직접 조작 + 이동 후 공격** — 판을 눌러 이동·공격, 주황 적 = 이동 후 공격.

## 지금 바로 (코어를 굵게)

### 1. 영입 의존도 완화 ★지금 최우선
4번째 동료 유무로 난이도가 급변한다 — 측정상 5단계 승률이 **3명 37% ↔ 4명 84%**. 적 특기를 넣은 지금도 그대로다.
- **왜 문제인가**: 마을이 길에 안 나오거나 금화가 모자라면 그 원정은 사실상 결정된다. 플레이어의 판단이 아니라 지도 운이 승패를 가른다.
- **손볼 후보**: 영입가 인하(🪙90 → 60) · 지도 생성에서 마을을 더 이르게 보장 · 인원이 3명이면 적 수를 하나 줄이는 보정.
- **작업 단위**: 고친 뒤 `__bqsim.campaign({runs:300})`으로 완주율이 30~45%에 남아 있는지 확인.

### 1.5. 방향키 칸 커서 (조작 되돌리기)
이동·공격을 메뉴에서 빼면서 **키보드만으로는 이동·공격을 할 수 없게 됐다.** 방향키로 칸 커서를 움직이고 Enter로 확정하는 경로가 필요하다.
- **왜**: `CLAUDE.md`의 "모든 선택 화면은 방향키로 다룰 수 있다" 규약을 전투 판만 못 지키고 있다.
- **어떻게**: 커서 칸 상태 하나 + 방향키 핸들러. 커서 칸을 노란 테두리로 그리고, Enter는 `onTile(cursor)`를 그대로 부르면 된다 — 마우스와 같은 경로라 로직 추가가 거의 없다.

### 2. 전투 연출 보강
이동 경로 따라 미끄러지기 · 피격 번쩍 · 데미지 숫자 튀어오르기 · 처치 연출 · 카메라 살짝 흔들기.
특기가 늘어난 지금은 "회전베기가 여럿을 벴다", "포효가 부하를 달궜다" 같은 순간을 **눈에 보이게** 하는 값어치가 커졌다.
- **왜**: 지금은 위치 보간만 있어서 "판정이 일어났다"는 감각이 약하다. 로직을 안 건드리고 체감을 크게 올리는 가성비 항목.
- **주의**: 연출은 **로직과 분리**할 것 — 연출을 기다리느라 상태 전이가 막히면 시뮬레이터가 못 돌린다.

## 그다음 (전투를 깊게)

### 3. 적 AI 고도화
지형 선점(숲·언덕) · 집중 공격 · 사제 우선 노리기 · 열세면 후퇴. **적이 특기는 쓰되 자리 싸움은 아직 단순하다** — 저격수가 거리를 벌리거나, 포효 뒤 부하가 몰아치는 식의 연계가 없다.
- 시뮬레이터로 "AI를 바꿨더니 승률이 어떻게 변했나"를 즉시 볼 수 있어 개발이 빠르다.

### 3.5. 직업당 두 번째 특기
지금은 직업마다 1종. 2종이 되면 "이번 턴에 어느 쪽을 쓸까"가 생겨 빌드 다양성이 한 겹 더 붙는다.
- 데이터만 추가하면 되는 구조다 (`SKILLS`에 항목 추가 + `ClassDef.skill`을 배열로). 넣을 때마다 `__bqsim.campaign()`으로 완주율을 다시 재는 게 규칙.

### 5. 진형/배치 단계
전투 시작 전 아군 시작 칸을 직접 고르는 단계.
- 구현량이 작은데 의사결정이 하나 늘어난다. 가성비 좋음.

### 6. 지형 상호작용
높이차에 따른 사거리·피해 보정 · 물가로 밀어내기 · 파괴 가능한 바위.

## 원정 층 (지도를 심심하지 않게)

### 7. 원정 사건 노드
갈림길에 이벤트 노드 — 용병 제안 · 함정 · 수수께끼 · 상인. 백층 던전의 방 이벤트와 같은 계열.

### 8. 저장 / 이어하기
원정 중간 상태를 `bq-run` 키로 localStorage에. 모바일에서 탭이 날아가도 이어갈 수 있게.

### 9. 동료 개성 · 상실
절차 생성 이름 + 소소한 특성. 죽으면 영영 잃는 규칙을 넣으면 원정에 감정적 무게가 생긴다.

## 마감 · 곁가지

### 10. 음악
절차 생성 BGM(지도 / 전투 / 승리). 백층 던전 `music.ts` 패턴 이식 — 외부 에셋 0개 원칙 유지.

### 11. 모바일 최적화
터치 타겟 크기 · 세로 화면 카메라 맞춤 · DPR 제한(고해상도 기기 발열 방지).

### 12. 바둑 · 오목식 미니게임
마을이나 특수 노드에서 즐기는 판 위 미니게임. 원작 영감 회수용.

### 13. 번들 크기 줄이기
현재 JS 번들 약 1.0MB(gzip 292KB) — 대부분 three.js다. 첫 로딩이 신경 쓰이면 three 모듈을 골라 import 하거나 타이틀 화면 이후로 동적 import.

---

## 어디부터 손댈지 고민된다면

적 특기까지 들어와 전투가 다채로워졌고 완주율도 40%대로 잡혔다. 다음은 **1번(영입 의존도)**을 권한다 —
지금 남은 가장 큰 구조적 결함이고("지도 운이 승패를 가른다"), 시뮬레이터로 바로 재며 고칠 수 있다.
전투의 새 순간들(회전베기·포효)을 눈에 보이게 하고 싶으면 2번(연출)이 로직을 안 건드려서 안전하다.
어느 쪽이든 밸런스가 흔들리면 `__bqsim.campaign()`으로 바로 확인된다.
